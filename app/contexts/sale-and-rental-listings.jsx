import {createContext} from 'react'

// [Tip]: Create a context to use it in the child level
const SaleAndRentalListingsContext = createContext(undefined)

export default SaleAndRentalListingsContext
